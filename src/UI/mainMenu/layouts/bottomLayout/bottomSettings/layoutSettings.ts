import {
  startBattleWidth,
  startBattleStartY
} from '../../../../../config/mainMenu';

export let startBattleButtonX:number = Math.round(startBattleWidth / 3) + 120;
export let startBattleButtonY:number = startBattleStartY + 15;
export let startBattleButtonWidth:number = 200;
export let startBattleButtonHeight:number = 40;
export let startBattleTextX:number = startBattleButtonX + 30;
export let startBattleTextY:number = startBattleButtonY + 30;
