import {
  armyLayoutWidth,
  titleHeight,
  unitRosterWidth
} from '../../../../../config/mainMenu';

export const width = 160;
export const height = 30;
export const battleSettingsX = armyLayoutWidth + unitRosterWidth + 50;
export const battleSettingsY = titleHeight;
export const unitInfoX = armyLayoutWidth + unitRosterWidth + 50 + width + 20;
export const unitInfoY = titleHeight;
