import Unit from '../../../unit/types/Unit';

/*
    calcuate unit weight (unit value on the battlefield)
    return number from 0 to 100, where 0 useless unit
    and 100 extremely valuable unit
*/
export const calculateUnitWeight = (unit:Unit):number => {
    let weight:number = 0;

    return weight;
}